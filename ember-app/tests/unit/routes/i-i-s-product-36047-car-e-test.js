import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Route | i-i-s-product-36047-car-e', function (hooks) {
  setupTest(hooks);

  // Replace this with your real tests.
  test('it exists', function(assert) {
    let route = this.owner.lookup('route:i-i-s-product-36047-car-e');
    assert.ok(route);
  });
});
